use strict;
use warnings;
use Data::Dump qw(dump);
use List::Util qw(zip min max sum);
use Tie::IxHash;

# ==========================================
# Librerías cargadas correctamente.
# Puedes usarlas en todos los programas.
# ==========================================

print "Librerías cargadas correctamente\n";
